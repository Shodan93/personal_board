# Kopiere diese Datei zu `.env` (oder trage die Werte in deine Claude-Config-`env` ein).
# WICHTIG: Der Service-Key umgeht RLS (Vollzugriff) — NIEMALS committen oder ins Frontend legen.

SUPABASE_URL=https://eqrzazmdamiplqiizrat.supabase.co
SUPABASE_SERVICE_KEY=sb_secret_DEIN_KEY_HIER
# Optional: Standard-Board, falls du nicht bei jedem Aufruf board_id angeben willst.
# Die UUID findest du über das Tool `list_boards`.
ORBIT_BOARD_ID=
